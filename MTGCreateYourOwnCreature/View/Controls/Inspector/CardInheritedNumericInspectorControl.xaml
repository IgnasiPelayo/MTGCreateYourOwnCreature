<UserControl x:Class="MTGCreateYourOwnCreature.View.Controls.Inspector.CardInheritedNumericInspectorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MTGCreateYourOwnCreature.View.Controls.Inspector"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <Grid Margin="5">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="75"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="{Binding Label, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   VerticalAlignment="Center"
                   Style="{StaticResource InspectorPropertyTitle}"/>

            <local:NumericSpinnerControl Grid.Column="1"
                             Value="{Binding LocalValue, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
        </Grid>
        
        <Grid Grid.Row="1">
            <Grid.Style>
                <Style TargetType="Grid">
                    <Setter Property="Visibility"
                    Value="Collapsed"/>

                    <Style.Triggers>
                        <DataTrigger Binding="{Binding HasInheritance, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="True">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Grid.Style>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="75"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Inherited"
                       VerticalAlignment="Center"
                       Style="{StaticResource InspectorPropertyTitle}"/>


            <Border Grid.Column="1"
                    Style="{StaticResource RoundedBorder}"
                    Height="20"
                    Margin="0,4,0,2"
                    Background="{StaticResource ControlBackgroundSelectedBrush}">

                <TextBlock Text="{Binding InheritedValue, RelativeSource={RelativeSource AncestorType=UserControl}}"
                           VerticalAlignment="Top"
                           HorizontalAlignment="Center"/>
            </Border>

            <TextBlock Grid.Row="1"
                       Text="Total"
                       VerticalAlignment="Center"
                       Style="{StaticResource InspectorPropertyTitle}"/>

            <Border Grid.Row="1"
                    Grid.Column="1"
                    Style="{StaticResource RoundedBorder}"
                    Height="20"
                    Margin="0,2,0,2"
                    Background="{StaticResource ControlBackgroundSelectedBrush}">

                <TextBlock Text="{Binding TotalValue, RelativeSource={RelativeSource AncestorType=UserControl}}"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Center"/>
            </Border>
        </Grid>
    </Grid>
</UserControl>
