<UserControl x:Class="MTGCreateYourOwnCreature.View.Controls.Inspector.CardManaCostInspectorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MTGCreateYourOwnCreature.View.Controls.Inspector"
             xmlns:mana="clr-namespace:MTGCreateYourOwnCreature.View.Controls.Mana"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <TextBlock Style="{StaticResource InspectorPropertyTitle}"
                   Text="Mana"/>

        <ItemsControl Grid.Row="1"
                      ItemsSource="{Binding Mana}">
        
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,5,10,0"
                          Height="20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="70"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>

                        </Grid.ColumnDefinitions>

                        <Viewbox Stretch="Uniform"
                                 StretchDirection="DownOnly"
                                 HorizontalAlignment="Right"
                                 Height="15"
                                 VerticalAlignment="Center">

                            <mana:ManaSymbolControl Value="{Binding PreviewManaSymbol.Value}"
                                                    Brush="{Binding PreviewManaSymbol.Brush}"/>
                        </Viewbox>

                        <local:NumericSpinnerControl Grid.Column="1"
                                                     Margin="10,0,0,0"
                                                     Value="{Binding Value}"/>

                        <Border Grid.Column="3"
                                VerticalAlignment="Top"
                                HorizontalAlignment="Right">
                            <mana:ManaCostControl ItemsSource="{Binding ManaSymbols}"
                                                  HorizontalAlignment="Right"/>
                        </Border>


                        <Border Grid.Column="4"
                                VerticalAlignment="Top"
                                HorizontalAlignment="Right">
                            <mana:ManaCostControl ItemsSource="{Binding InheritedManaSymbols}"
                                                  HorizontalAlignment="Right"/>
                        </Border>

                    </Grid>
                    
                </DataTemplate>
            </ItemsControl.ItemTemplate>
            
        </ItemsControl>

    </Grid>

</UserControl>
