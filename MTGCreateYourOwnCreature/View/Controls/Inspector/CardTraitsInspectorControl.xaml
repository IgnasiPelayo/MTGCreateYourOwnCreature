<UserControl x:Class="MTGCreateYourOwnCreature.View.Controls.Inspector.CardTraitsInspectorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MTGCreateYourOwnCreature.View.Controls.Inspector"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <Grid Margin="5">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding Label, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   VerticalAlignment="Center"
                   Style="{StaticResource InspectorPropertyTitle}"/>


        <Border Style="{StaticResource RoundedBorder}"
                Grid.Row="1"
                Margin="0,5,0,0"
                MinHeight="25">
            <Grid VerticalAlignment="Center">
                <TextBox x:Name="PART_Input"
                         Text="{Binding NewValue, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                         Margin="8,0,0,0"
                         Background="Transparent"
                         BorderThickness="0">

                    <TextBox.InputBindings>
                        <KeyBinding Key="Enter" Command="{Binding AddCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                    </TextBox.InputBindings>
                </TextBox>

                <TextBlock IsHitTestVisible="False"
                           Opacity="0.45"
                           Margin="10,0,0,0">
                    
                    <TextBlock.Text>
                        <MultiBinding StringFormat="Add {0}...">
                            <Binding Path="Label" RelativeSource="{RelativeSource AncestorType=UserControl}"/>
                        </MultiBinding>
                    </TextBlock.Text>

                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>

                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Text, ElementName=PART_Input}" Value="">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>
        </Border>
            
        <ItemsControl Grid.Row="2"
                      Margin="0,5,0,0"
                      ItemsSource="{Binding ItemsSource, RelativeSource={RelativeSource AncestorType=UserControl}}">

            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>


            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Margin="0,0,2,0"
                            Padding="8,4"
                            CornerRadius="6"
                            BorderThickness="1"
                            Background="{StaticResource ControlBackgroundBrush}">

                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>

                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsInherited}" Value="True">
                                        <Setter Property="BorderBrush" Value="{StaticResource ControlInheritedBorderBrush}"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>

                        <StackPanel Orientation="Horizontal"
                                    VerticalAlignment="Center">

                            <TextBlock Text="{Binding Value}"/>

                            <Button Margin="6,0,0,0"
                                    Width="16"
                                    Height="16"
                                    Padding="0"
                                    Content="✕"
                                    FontSize="10"
                                    FontWeight="Bold"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    Cursor="Hand"
                                    FocusVisualStyle="{x:Null}"
                                    Command="{Binding RemoveCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding}">

                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Opacity" Value="0.7"/>

                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>

                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Foreground" Value="#FF7070"/>
                                                <Setter Property="Opacity" Value="1"/>
                                            </Trigger>

                                            <DataTrigger Binding="{Binding IsInherited}" Value="True">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
