<UserControl x:Class="MTGCreateYourOwnCreature.View.Controls.ManaCostControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MTGCreateYourOwnCreature.View.Controls"
             xmlns:converters="clr-namespace:MTGCreateYourOwnCreature.View.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <converters:ManaToSymbolsConverter x:Key="ManaConverter"/>
    </UserControl.Resources>

    <Viewbox Stretch="Uniform"
             StretchDirection="DownOnly"
             HorizontalAlignment="Right">

        <ItemsControl ItemsSource="{Binding TotalMana, Converter={StaticResource ManaConverter}}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <local:ManaSymbolControl Brush="{Binding Brush}"
                                             Value="{Binding Value}"
                                             Padding="2,0,2,0"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>

        </ItemsControl>

    </Viewbox>

</UserControl>
